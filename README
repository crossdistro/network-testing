Network Test Report
===================

Generates a static page summarizing test results.

Usage: build.py [OPTIONS]

Options:
  -o, --outdir TEXT
  --datadir TEXT
  --templatedir TEXT
  --help              Show this message and exit.

Example data is included in the `data` directory.
